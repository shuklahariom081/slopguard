# SlopGuard Backend — optimized for Railway free tier

# API framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Supabase
supabase>=2.7.0

# ML — CPU only (much smaller than full torch)
transformers>=4.44.0
torch==2.3.0+cpu
torchvision==0.18.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Vision
pillow>=10.4.0
numpy>=1.26.0

# Utilities
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic>=2.8.0
httpx>=0.27.0